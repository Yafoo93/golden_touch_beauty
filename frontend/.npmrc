# Tailwind/PostCSS and TypeScript are build-time dependencies. Render may set
# production-oriented npm defaults during a build, so explicitly retain dev
# dependencies for `npm ci`; Next.js prunes nothing from the deployed server
# based on this setting.
include=dev
